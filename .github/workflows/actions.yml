name: tmux_interface github actions

on:
  push:
    branches:
      - main
      - dev
  #pull_request:
    #branches:
    # - main
    # - dev

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Build
      run: cargo build --verbose
    - name: Run tests
      run: cargo test --verbose
